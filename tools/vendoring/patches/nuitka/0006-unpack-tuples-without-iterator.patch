From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Kevin Turcios <turcioskevinr@gmail.com>
Date: Thu, 24 Sep 2026 04:31:35 -0400
Subject: [PATCH] Optimization: Unpack tuples of the unpacked size without an
 iterator

Unpacking always created an iterator, and fetched every value through
it, then checked it was exhausted. CPython 3.11+ reads a tuple of the
right size directly instead, so compiled code was slower than the
interpreter at "a, b = t" and "for k, v in d.items()".

When all values are unpacked, i.e. there is no starred target, a tuple
of exactly that size now stands in for the iterator. "UNPACK_NEXT" and
the unpack check recognize it, since no real iterator is an exact tuple,
and read the values by index. Anything else, including tuple subclasses
and wrong sizes, takes the iterator path as before, with the same
errors.

A 4 value unpack in a loop takes 2.5 instead of 4.5 ms and a loop over
"dict.items()" 2.9 instead of 4.9 ms, where CPython 3.14 takes 3.0 and
2.8 ms.

Co-authored-by: wtfyd-agent[bot] <328989958+wtfyd-agent[bot]@users.noreply.github.com>
Signed-off-by: Kevin Turcios <turcioskevinr@gmail.com>
---
 .../build/include/nuitka/helper/iterators.h   | 36 +++++++++++++++-
 nuitka/code_generation/IteratorCodes.py       | 42 ++++++++++++++++++-
 nuitka/nodes/BuiltinIteratorNodes.py          | 16 +++++++
 .../tree/ReformulationAssignmentStatements.py | 10 +++--
 4 files changed, 99 insertions(+), 5 deletions(-)

diff --git a/nuitka/build/include/nuitka/helper/iterators.h b/nuitka/build/include/nuitka/helper/iterators.h
index 173251e31..cb8b71892 100644
--- a/nuitka/build/include/nuitka/helper/iterators.h
+++ b/nuitka/build/include/nuitka/helper/iterators.h
@@ -173,6 +173,20 @@ NUITKA_MAY_BE_UNUSED static PyObject *MAKE_UNPACK_ITERATOR(PyObject *iterated) {
     }
 }
 
+// For unpacking all of "count" values: a tuple of exactly that size is its own
+// "iterator", which "UNPACK_NEXT" and "UNPACK_ITERATOR_CHECK" read by index. No
+// real iterator is an exact tuple, so the two cannot be confused.
+NUITKA_MAY_BE_UNUSED static PyObject *MAKE_UNPACK_ITERATOR_SIZED(PyObject *iterated, Py_ssize_t count) {
+    CHECK_OBJECT(iterated);
+
+    if (PyTuple_CheckExact(iterated) && PyTuple_GET_SIZE(iterated) == count) {
+        Py_INCREF(iterated);
+        return iterated;
+    }
+
+    return MAKE_UNPACK_ITERATOR(iterated);
+}
+
 #endif
 
 NUITKA_MAY_BE_UNUSED static PyObject *ITERATOR_NEXT_ITERATOR(PyObject *iterator) {
@@ -240,8 +254,15 @@ NUITKA_MAY_BE_UNUSED static PyObject *BUILTIN_NEXT2(PyThreadState *tstate, PyObj
 }
 
 // For cases, where no exception raising is needed, because we know it at compile time.
-NUITKA_MAY_BE_UNUSED static PyObject *UNPACK_NEXT_INFALLIBLE(PyObject *iterator) {
+NUITKA_MAY_BE_UNUSED static PyObject *UNPACK_NEXT_INFALLIBLE(PyObject *iterator, Py_ssize_t index) {
     CHECK_OBJECT(iterator);
+
+    if (PyTuple_CheckExact(iterator)) {
+        PyObject *result = PyTuple_GET_ITEM(iterator, index);
+        Py_INCREF(result);
+        return result;
+    }
+
     assert(HAS_ITERNEXT(iterator));
 
     PyObject *result = (*Py_TYPE(iterator)->tp_iternext)(iterator);
@@ -260,6 +281,14 @@ NUITKA_MAY_BE_UNUSED static PyObject *UNPACK_NEXT(PyThreadState *tstate,
 #endif
 {
     CHECK_OBJECT(iterator);
+
+    // A tuple of the unpacked size, see "MAKE_UNPACK_ITERATOR_SIZED".
+    if (PyTuple_CheckExact(iterator)) {
+        PyObject *result = PyTuple_GET_ITEM(iterator, seq_size_so_far);
+        Py_INCREF(result);
+        return result;
+    }
+
     assert(HAS_ITERNEXT(iterator));
 
     PyObject *result = (*Py_TYPE(iterator)->tp_iternext)(iterator);
@@ -378,6 +407,11 @@ SET_UNPACK_TOO_MANY_VALUES_ERROR(PyThreadState *tstate, struct Nuitka_ExceptionP
 NUITKA_MAY_BE_UNUSED static bool UNPACK_ITERATOR_CHECK(PyThreadState *tstate,
                                                        struct Nuitka_ExceptionPreservationItem *exception_state,
                                                        PyObject *iterator, int expected) {
+    // A tuple of the unpacked size, see "MAKE_UNPACK_ITERATOR_SIZED".
+    if (PyTuple_CheckExact(iterator)) {
+        return true;
+    }
+
     PyObject *attempt = (*(Py_TYPE(iterator)->tp_iternext))(iterator);
 
     if (likely(attempt == NULL)) {
diff --git a/nuitka/code_generation/IteratorCodes.py b/nuitka/code_generation/IteratorCodes.py
index fe1734460..595671567 100644
--- a/nuitka/code_generation/IteratorCodes.py
+++ b/nuitka/code_generation/IteratorCodes.py
@@ -232,7 +232,10 @@ def generateSpecialUnpackCode(to_name, expression, emit, context):
         needs_check = expression.mayRaiseExceptionOperation()
 
         if not needs_check:
-            emit("%s = UNPACK_NEXT_INFALLIBLE(%s);" % (result_name, value_name))
+            emit(
+                "%s = UNPACK_NEXT_INFALLIBLE(%s, %d);"
+                % (result_name, value_name, expression.getCount() - 1)
+            )
         elif python_version < 0x350:
             (
                 exception_state_name,
@@ -467,6 +470,43 @@ def generateBuiltinIter1Code(to_name, expression, emit, context):
 
 def generateBuiltinIterForUnpackCode(to_name, expression, emit, context):
     may_raise = expression.mayRaiseExceptionOperation()
+    count = expression.getCount()
+
+    if may_raise and count is not None:
+        # All values are unpacked, so a tuple of the right size can be used
+        # directly instead of an iterator, which "UNPACK_NEXT" and the unpack
+        # check recognize.
+        value_name = context.allocateTempName("unpack_iterated")
+
+        generateExpressionCode(
+            to_name=value_name,
+            expression=expression.subnode_value,
+            emit=emit,
+            context=context,
+        )
+
+        with withObjectCodeTemporaryAssignment(
+            to_name, "unpack_iterator", expression, emit, context
+        ) as result_name:
+            context.setCurrentSourceCodeReference(
+                expression.getCompatibleSourceReference()
+            )
+
+            emit(
+                "%s = MAKE_UNPACK_ITERATOR_SIZED(%s, %d);"
+                % (result_name, value_name, count)
+            )
+
+            getErrorExitCode(
+                check_name=result_name,
+                release_name=value_name,
+                emit=emit,
+                context=context,
+            )
+
+            context.addCleanupTempName(result_name)
+
+        return
 
     generateCAPIObjectCode(
         to_name=to_name,
diff --git a/nuitka/nodes/BuiltinIteratorNodes.py b/nuitka/nodes/BuiltinIteratorNodes.py
index 47b078cad..fdb7a8e15 100644
--- a/nuitka/nodes/BuiltinIteratorNodes.py
+++ b/nuitka/nodes/BuiltinIteratorNodes.py
@@ -353,6 +353,22 @@ class ExpressionBuiltinZip310(
 class ExpressionBuiltinIterForUnpack(ExpressionBuiltinIter1):
     kind = "EXPRESSION_BUILTIN_ITER_FOR_UNPACK"
 
+    __slots__ = ("count",)
+
+    def __init__(self, value, source_ref, count=None):
+        ExpressionBuiltinIter1.__init__(self, value=value, source_ref=source_ref)
+
+        # Number of values unpacked, if all of them are, i.e. there is no
+        # starred target. Allows the C code to unpack a tuple of that size
+        # directly, without creating an iterator.
+        self.count = count
+
+    def getDetails(self):
+        return {"count": self.count}
+
+    def getCount(self):
+        return self.count
+
     def computeExpression(self, trace_collection):
         result = self.subnode_value.computeExpressionIter1(
             iter_node=self, trace_collection=trace_collection
diff --git a/nuitka/tree/ReformulationAssignmentStatements.py b/nuitka/tree/ReformulationAssignmentStatements.py
index 4af479afe..b9a9d92e9 100644
--- a/nuitka/tree/ReformulationAssignmentStatements.py
+++ b/nuitka/tree/ReformulationAssignmentStatements.py
@@ -329,14 +329,18 @@ not enough values to unpack (expected at least %d, got %%d)""" % (len(statements
             )
 
         if python_version >= 0x370:
-            iter_creation_class = ExpressionBuiltinIterForUnpack
+            iter_creation = ExpressionBuiltinIterForUnpack(
+                value=source,
+                count=len(detail) if starred_list_var is None else None,
+                source_ref=source_ref,
+            )
         else:
-            iter_creation_class = ExpressionBuiltinIter1
+            iter_creation = ExpressionBuiltinIter1(value=source, source_ref=source_ref)
 
         statements = [
             makeStatementAssignmentVariable(
                 variable=source_iter_var,
-                source=iter_creation_class(value=source, source_ref=source_ref),
+                source=iter_creation,
                 source_ref=source_ref,
             ),
             makeTryFinallyReleaseStatement(
